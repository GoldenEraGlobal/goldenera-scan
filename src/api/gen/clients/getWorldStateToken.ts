/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@kubb/plugin-client/clients/axios'
import type {
  GetWorldStateTokenQueryResponse,
  GetWorldStateTokenPathParams,
  GetWorldStateToken400,
  GetWorldStateToken401,
  GetWorldStateToken404,
  GetWorldStateToken409,
  GetWorldStateToken413,
  GetWorldStateToken500,
} from '../types/GetWorldStateToken.ts'
import type {
  RequestConfig,
  ResponseErrorConfig,
} from '@kubb/plugin-client/clients/axios'

function getGetWorldStateTokenUrl(
  address: GetWorldStateTokenPathParams['address'],
) {
  const res = {
    method: 'GET',
    url: `/api/core/v1/blockchain/worldstate/token/${address}` as const,
  }
  return res
}

/**
 * {@link /api/core/v1/blockchain/worldstate/token/:address}
 */
export async function getWorldStateToken(
  address: GetWorldStateTokenPathParams['address'],
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    GetWorldStateTokenQueryResponse,
    ResponseErrorConfig<
      | GetWorldStateToken400
      | GetWorldStateToken401
      | GetWorldStateToken404
      | GetWorldStateToken409
      | GetWorldStateToken413
      | GetWorldStateToken500
    >,
    unknown
  >({
    method: 'GET',
    url: getGetWorldStateTokenUrl(address).url.toString(),
    ...requestConfig,
  })
  return res.data
}
