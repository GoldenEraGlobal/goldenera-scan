/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@kubb/plugin-client/clients/axios'
import type {
  GetMetricsQueryResponse,
  GetMetrics400,
  GetMetrics401,
  GetMetrics404,
  GetMetrics409,
  GetMetrics413,
  GetMetrics500,
} from '../types/GetMetrics.ts'
import type {
  RequestConfig,
  ResponseErrorConfig,
} from '@kubb/plugin-client/clients/axios'

function getGetMetricsUrl() {
  const res = { method: 'GET', url: `/api/shared/v1/metrics` as const }
  return res
}

/**
 * {@link /api/shared/v1/metrics}
 */
export async function getMetrics(
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    GetMetricsQueryResponse,
    ResponseErrorConfig<
      | GetMetrics400
      | GetMetrics401
      | GetMetrics404
      | GetMetrics409
      | GetMetrics413
      | GetMetrics500
    >,
    unknown
  >({ method: 'GET', url: getGetMetricsUrl().url.toString(), ...requestConfig })
  return res.data
}
